<view class="jibian-bottom-nav-shell">
  <view class="jibian-bottom-nav-card">
    <block wx:for="{{tabs}}" wx:key="key">
      <view
        class="jibian-bottom-nav-item {{selected === index ? 'jibian-bottom-nav-item-active' : ''}}"
        style="left: {{item.leftRpx}}rpx;"
        data-index="{{index}}"
        bindtap="handleTap"
        hover-class="jibian-bottom-nav-pressed"
      >
        <view class="jibian-bottom-nav-active-bg" wx:if="{{selected === index}}">
          <view class="jibian-bottom-nav-active-shine"></view>
        </view>
        <text class="jibian-bottom-nav-label">{{item.label}}</text>
      </view>
    </block>
  </view>
</view>
